apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-k8s-demo-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: node.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: node-k8s-demo-service
                port:
                  number: 3000

